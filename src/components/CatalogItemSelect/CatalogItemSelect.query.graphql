query CatalogItemSelect($input: SearchInput) {
    search(input: $input) {
        nodes {
            id
            label
        }
        pageInfo {
            ...PageInfo
        }
        totalElements
    }
}
