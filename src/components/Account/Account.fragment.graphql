fragment Account on Account {
    username
    created
    lastModified
    locked
    expired
    credentialsExpired
    emailConfirmed
    enabled
    status
    profile {
        ...UserProfile
    }
}
