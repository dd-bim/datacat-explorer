query FacetList($input: FilterInput) {
    facets(input: $input) {
        nodes {
            ...Facet
        }
        pageInfo {
            ...PageInfo
        }
        totalElements
    }
}

query Facet($id: ID!) {
    facet(id: $id) {
        ...Facet
    }
}
