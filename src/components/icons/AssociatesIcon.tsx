import {ImportExport} from "@material-ui/icons";
import React from "react";
import {SvgIconProps} from "@material-ui/core";

export default function AssociatesRelationIcon(props: SvgIconProps) {
  return (
    <ImportExport {...props} />
  );
}
