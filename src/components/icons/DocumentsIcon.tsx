import React from 'react';
import {Assignment} from '@material-ui/icons';
import {SvgIconProps} from "@material-ui/core";

export default function DocumentsRelationIcon(props: SvgIconProps) {
    return <Assignment {...props} />
}
