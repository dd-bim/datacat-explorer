import React from 'react';
import {GroupWork} from '@material-ui/icons';
import {SvgIconProps} from "@material-ui/core";

export default function GroupsIcon(props: SvgIconProps) {
    return <GroupWork {...props} />
}
